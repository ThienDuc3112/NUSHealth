import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SharedPlan = () => {
  return (
    <View>
      <Text>SharedPlan</Text>
    </View>
  );
};

export default SharedPlan;

const styles = StyleSheet.create({});
