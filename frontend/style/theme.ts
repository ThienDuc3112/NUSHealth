import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
  input: {
    padding: 8,
    borderBottomColor: "gray",
    borderBottomWidth: 1,
    marginHorizontal: 8,
    marginBottom: 4,
    height: 40,
  },
});
